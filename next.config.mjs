/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "lh3.googleusercontent.com"], 
    // Unsplash images + Google OAuth profile pics
  },
};

export default nextConfig;
